---
// Support-tickets settings section, rendered by the base Einstellungen host via
// virtual:panel-settings. Notification toggles (CP4); the status-registry editor
// + IMAP config land in later checkpoints.
import NotificationSettings from "./NotificationSettings.tsx";
---

<div class="tds-settings-section__body">
  <h3>E-Mail-Benachrichtigungen</h3>
  <NotificationSettings client:load />
</div>
